"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = _default;

var _requestPromise = _interopRequireDefault(require("request-promise"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _default(cb) {
  var url = 'https://api.overwatchleague.com/v2/standings';
  var options = {
    uri: encodeURI(url),
    encoding: 'utf8',
    json: true
  };
  (0, _requestPromise["default"])(options).then(function (resp) {
    var json = {
      data: transform(resp.data)
    };
    cb(null, json);
  })["catch"](function (err) {
    cb(err);
  });
}

function transform(data) {
  var includes = ['id', 'divisionId', 'name', 'abbreviatedName', 'league', 'stages', 'preseason']; // Filter only the properties we want to use.

  var filtered = data.map(function (o) {
    return Object.keys(o).filter(function (key) {
      return includes.includes(key);
    }).reduce(function (obj, key) {
      obj[key] = o[key];
      return obj;
    }, {});
  });
  return filtered;
}